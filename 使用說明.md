# 自動剪片小工房 — 使用說明

丟影片進來（一支或很多支），自動幫你：**串接、上字幕、配語音、混背景音樂**，橫式直式都能出。

## 影片放哪？——「素材」資料夾

拍好、下載好的影片全部丟進 **`素材`** 資料夾就對了。怕亂的話，雙擊 **`整理素材.bat`**，它會自動標上時間、收進月份資料夾：

```
素材/隨手拍.mp4  →  素材/2026-07/2026-07-25_1430_隨手拍.mp4
```

（時間抓的是影片的**拍攝時間**，手機拍的都有；已整理過的不會重複搬。）

之後不管在哪都不用貼路徑：

- **口說剪片台**：面板上方的「📂 素材庫」直接列出所有素材（新→舊），點一下就開
- **拖拉問答模式**：會列出最新的素材讓你**輸入編號**挑選

## 最簡單的用法

把影片**拖到 `拖影片到這裡.bat` 上面**（可以一次選好幾支一起拖！），它會用問答帶你走完：

1. 好幾支影片？→ 自動統一畫質串接成一支（可選淡入淡出轉場），還能調順序
2. 影片裡有人講話？→ 自動聽打上字幕（自動轉繁體）
3. 無旁白素材？→ 給文字稿，AI 配音＋字幕
4. 要不要背景音樂（人聲出現時音樂會自動變小聲）
5. 橫式／直式 9:16／都要

成品會出現在 **`輸出`** 資料夾。

## 想邊看邊改？用口說剪片台

雙擊 **`口說剪片台.bat`**，瀏覽器會打開一個面板。貼上影片路徑按「開啟」，然後：

**按住空白鍵**（或按住那顆麥克風鈕）講你的需求，放開就會辨識。例如：

| 你講什麼 | 它做什麼 |
|---|---|
| 「這裡加字幕買一送一」 | 在當下位置加字幕 |
| 「從這裡開始」→「剪到這裡」 | 剪掉這兩點之間 |
| 「剪掉前面五秒」 | 剪掉當下往前五秒 |
| 「這段加快兩倍」 | 變速 |
| 「這裡靜音」／「小聲一點」 | 調聲音 |

右邊會列出所有操作（時間軸也會標記），可以刪掉重來。都講完了按「開始算圖」，剪好的檔案一樣在 `輸出`。

> 聽你講話是**本地的 Whisper 在跑，不用網路也不花錢**。真的聽不懂的句子會標成「待解讀」，
> 可以按旁邊的鈕複製下來丟給 Claude 幫你翻成操作。
>
> 剪掉、變速之後字幕的時間點會**自動重新對齊**，不用自己算。

## 用自己的聲音（音色複製）

給一段自己的錄音當樣本，就能讓它用你的音色念稿子：

```bash
.venv_voiceclone/Scripts/python.exe voicecut/voiceclone.py ^
  --ref 素材/我的聲音/_參考音色/ref_全段.wav ^
  --script 稿子.txt --out 輸出/旁白.wav
```

### 讓聲音有情感

| 參數 | 作用 |
|---|---|
| `--temperature 0.95` | 語調起伏。預設 0.75 平穩，0.9~0.95 有戲但偶爾念錯字 |
| `--top-p 0.94` | 取樣範圍，調高語氣更多樣 |
| `--rep-penalty 6` | 調低語氣較放鬆（預設 10） |
| `--breath 素材/我的聲音/_呼吸聲` | 句子之間插入**你真實的呼吸聲** |
| `--seed 42` | 固定隨機種子，同一次結果可重現 |

呼吸聲要先切出來（只要做一次）：

```bash
python voicecut/breath.py 素材/我的聲音/你的錄音.m4a
```

### 句尾語氣標記

稿子的句尾加標記，就能指定那句話要怎麼收尾：

```
欸，這是我家窗外喔——晚上十一點多了啦。{拖}
你看對面那棟大樓，居然還有幾戶沒睡欸！{揚}{強}
有時候就這樣看看，也不錯啊。{拖}{弱}
```

| 標記 | 效果 |
|---|---|
| `{拖}` | 尾音拉長（音高不變） |
| `{揚}` | 尾音音高往上爬（驚訝、疑問） |
| `{降}` | 尾音往下掉（感嘆、放鬆） |
| `{強}` | 整句變大聲（強調） |
| `{弱}` | 整句變小聲（喃喃自語） |

可以疊加。另外**多加語助詞**（欸、啊、喔、啦）效果比調參數更明顯。

### 一定要驗證念字

溫度調高後偶爾會念錯多音字（實測遇過「看**著**」念成「探住」、「**停**好了」念成「挺好的」）。合成完跑這個檢查：

```bash
python voicecut/checkspeech.py 輸出/旁白.wav 稿子.txt
```

它會用 Whisper 聽打回來跟原稿比對，列出念錯／漏念／多念的地方。念錯就**換個沒有歧義的詞**，不要降溫度（會失去情感）。

> ⚠️ XTTS-v2 模型授權是 CPML，**禁止商業用途**。

## 配音聲音（11 種）

台灣腔男女聲、大陸腔、粵語都有，`python autocut.py --list-voices` 看清單。

**雙人對話**：文字稿行首寫 `[男]`、`[活潑女]`、`[粵語女]` 之類就能切換聲音，例如：

```
[溫和女] 大家好，今天要介紹一個好用的工具。
[男] 真的假的，快講重點！
```

## 背景音樂放哪？

把你的 mp3／wav 丟進 **`背景音樂`** 資料夾，跑的時候就能選，
也可以選「隨機挑一首」。

> 找免費音樂可以去：YouTube 音效庫、Pixabay Music、Free Music Archive

## 指令用法（進階）

```
python autocut.py 影片.mp4                              模式A：聽打字幕，橫式
python autocut.py 片1.mp4 片2.mp4 片3.mp4                多支依序串接再上字幕
python autocut.py 影片.mp4 --layout both --music auto   橫直都出＋隨機配樂
python autocut.py 素材.mp4 --script 稿子.txt --layout v  模式B：TTS配音，直式
python autocut.py 影片.mp4 --subs 改好的.srt             用改過的字幕重燒
```

常用選項：

| 選項 | 說明 |
|---|---|
| `--transition fade / none` | 多支串接的轉場：淡入淡出／直接相接 |
| `--layout h / v / both` | 橫式／直式 9:16／都要 |
| `--music 檔案.mp3` 或 `auto` | 背景音樂（auto＝資料夾隨機挑） |
| `--music-vol 0.22` | 音樂音量（0~1） |
| `--model small / medium / large-v3` | 字幕辨識：快 / 預設 / 最準 |
| `--voice hsiaochen / hsiaoyu / yunjhe` | 配音：溫和女聲／活潑女聲／沉穩男聲 |
| `--rate +10%` | 配音語速（減速要寫 `--rate=-10%`） |
| `--keep-ambient` | 配音模式保留素材原本的環境音 |
| `--narration 旁白.wav` | 配音模式重燒用：搭配 `--subs`，只修字幕不重新配音 |

## 常見問題

- **第一次跑很久？** 語音辨識模型（medium）已經幫你下載好了，直接用即可；換別的模型才需要再下載。
- **字幕有錯字？** 打開 `輸出/xxx_字幕.srt` 改一改，再用 `--subs` 重燒一次，不用重新聽打。
  配音模式（模式B）會多存一個 `xxx_旁白.wav`，重燒時加 `--narration` 帶上它就不用重新配音。
- **TTS 配音需要網路**（用微軟的線上語音），聽打字幕則完全離線。
- **直式影片長怎樣？** 上下自動補模糊背景、字幕用大字卡置中偏下，適合 Shorts／TikTok。
- **電腦沒顯卡會不會很慢？** 聽打是 CPU 跑，大約影片長度的 1~3 倍時間；趕時間用 `--model small`。
