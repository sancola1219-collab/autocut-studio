# UI 設計藍圖 — 「像剪輯軟體」的網頁工作台

> 狀態：**設計紀錄，尚未動工**（2026-07-31 與使用者討論後存檔）。
> 目的：讓任何接手的人（包括未來的 Claude）知道要做成什麼樣子、怎麼分期。
> 前提：後端全部已存在（autocut.py + voicecut/*），UI 只是把現有能力接上畫面。

## 一、定位：能做到什麼、不做什麼

**能做到（跟剪映像的部分）**
- 時間軸：素材縮圖橫排、拖曳排序、每段顯示長度、旁白波形對照
- 素材庫側欄：影片＋照片縮圖牆，勾選加入時間軸
- 屬性面板：點選某一段 → 右側調整（這段的轉場、照片秒數）
- 全域設定：聲音人設、字幕風格、背景音樂、直式橫式
- 成品預覽：算完直接在面板播放、下載

**做不到／刻意不做（跟剪映不像的部分）**
- ❌ 即時預覽（無 GPU，任何變更都要重算，假裝即時只會又卡又騙人）
- ❌ 逐幀精修、關鍵幀動畫（ffmpeg 批次管線不適合，要精修去用剪映）
- ❌ 多軌疊加自由擺放（我們的強項是「自動」，不是「手動自由度」）

**設計哲學**：剪映是「手動剪、所見即所得」；我們是「排好料、AI 全自動、一鍵出片」。
UI 要圍繞第二種，不要模仿第一種。

## 二、畫面配置（單頁三欄）

```
┌────────────┬──────────────────────────────┬────────────┐
│  素材庫     │        時間軸（主角）           │  屬性面板    │
│            │                              │            │
│ [縮圖牆]    │  ┌──┐┌──┐┌──┐┌──┐┌──┐        │ 選中段落：   │
│ ☑ 門口.mp4 │  │縮││縮││縮││縮││縮│ ← 拖曳排序│  轉場 [▼]   │
│ ☑ 大廳.mp4 │  └──┘└──┘└──┘└──┘└──┘        │  照片秒數    │
│ ☐ 產品.jpg │  8.5s 11s 4s📷 15s 9s         │  [預覽格]    │
│            │  ───────────────────────      │            │
│ [↻ 整理素材]│  🎙 旁白稿（分段＝換場景）        │ 全域：      │
│            │  ┌──────────────────────┐    │  聲音 [▼19] │
│            │  │第一段文案…            │    │  🔊 試聽    │
│            │  │                      │    │  風格 [▼5]  │
│            │  │第二段文案…            │    │  音樂 [▼]   │
│            │  └──────────────────────┘    │  版式 ○直○橫 │
│            │  [讓 Claude 寫文案]           │            │
├────────────┴──────────────────────────────┴────────────┤
│  [🎬 一鍵成片]  進度：配音 3/8 ▓▓▓░░ → 對齊 → 合成        │
│  完成 → [▶ 播放] [⬇ 下載] [🔁 只改字幕重燒] [📤 發布包]    │
└─────────────────────────────────────────────────────────┘
```

要點：
- **時間軸每段一張縮圖**（後端抽中間幀），照片段落標 📷 並可調秒數
- **旁白稿放時間軸正下方**：空行分段＝場景切換，段數與素材數的對應
  即時顯示（「4 段文案 / 5 支素材 ⚠ 差一段」），這是 fitclips 的規則視覺化
- **試聽鈕**：選人設後唸一句固定短句（快取），不用整篇配完才知道聲音對不對
- **進度條走真實階段**：配音(逐句)→驗字→對齊→串接→合成，各階段後端本來就有 print，
  接 SSE 或輪詢丟到前端就行

## 三、使用流程（user journey）

1. 開面板 → 素材庫自動列出（既有 /library API）→ 勾選 → 進時間軸 → 拖排序
2. 貼文案（或按「讓 Claude 寫」→ 這步之後接 API 或先人工）
3. 右欄選：聲音人設（試聽）、字幕風格、轉場（全域或逐段）、音樂、版式
4. 按「一鍵成片」→ 後端跑 voiceclone --verify → fitclips → autocut → 進度條直播
5. 完成 → 面板內播放 → 不滿意改哪裡就改哪裡 → 「只改字幕重燒」走快速路徑
6. 滿意 → 「發布包」＝成品＋封面幀＋文案 txt 一個資料夾

## 四、技術對應（UI 元件 → 既有後端）

| UI 元件 | 後端（已存在） |
|---|---|
| 素材庫縮圖牆 | server.py /library ＋ ffmpeg 抽幀 |
| 時間軸段落資訊 | probe_video / probe_vstream_duration |
| 照片段落 | photos.ken_burns（--photo-dur） |
| 聲音人設下拉＋試聽 | voices.PERSONAS ＋ tts_narration 單句 |
| 字幕風格下拉 | styles.STYLES（可加：抽一幀套字幕當預覽圖） |
| 轉場下拉（分組） | transitions.GROUPS |
| 一鍵成片 | voiceclone --verify → fitclips → autocut（今天手動跑的鏈） |
| 進度直播 | 各工具的 print → 子程序 stdout → SSE/輪詢 |
| 只改字幕重燒 | --subs ＋ --narration 快速路徑 |
| 口說修改 | 既有 PTT（空白鍵）＋ parser.py，沿用 |

新增後端只需要：`/timeline_thumb`（抽縮圖）、`/tts_preview`（單句試聽）、
`/pipeline`（把三步鏈包成一個帶進度的工作）。其餘全是現成的。

## 五、分期（真要做的時候照這個順序）

- **P1 一鍵成片（核心，約一個工作天）**：素材勾選＋排序、文案框、
  三個下拉（人設/風格/轉場）、一鍵成片＋進度條、完成播放下載。
  沒有縮圖時間軸也能用，先求「不靠 Claude 也能出片」。
- **P2 時間軸體驗（半天）**：縮圖、拖曳排序、逐段轉場、照片秒數、段數對應提示。
- **P3 加分項**：試聽快取、風格預覽圖、發布包一鍵匯出、口說整合到同一頁。

## 六、風險備忘

- CPU 合成慢是體感瓶頸 → 進度條一定要真實分階段，別讓使用者以為當機
- 同時只能跑一條管線（既有 _job_lock 模式沿用），連點要 409
- 檔名中文＋空格：全走 pathlib，別讓前端傳來的路徑進 shell
- 口說剪片台既有功能不能弄壞：新 UI 做成**新分頁**，舊頁面原樣保留
```
